apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Intentionally empty. Used as the `path` for wave-gate Kustomizations, which
# exist only to aggregate `dependsOn` for a batch of apps (see clusters/*/tiers/).
resources: []
