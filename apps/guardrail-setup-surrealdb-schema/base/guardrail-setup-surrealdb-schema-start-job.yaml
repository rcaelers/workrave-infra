apiVersion: batch/v1
kind: Job
metadata:
  name: guardrail-setup-surrealdb-schema-start
  annotations:
    kustomize.toolkit.fluxcd.io/force: "true"
spec:
  backoffLimit: 6
  template:
    metadata:
      labels:
        app: guardrail-setup-surrealdb-schema
    spec:
      restartPolicy: OnFailure
      containers:
        - name: schema-rollout-start
          image: ghcr.io/rcaelers/guardrail-schema-sync:placeholder
          command: ["sh", "/app/bin/surrealkit-rollout-start.sh"]
          env:
            - name: DATABASE_HOST
              value: ws://surrealdb.surrealdb:8000
            - name: DATABASE_NAMESPACE
              value: guardrail
            - name: DATABASE_NAME
              value: guardrail
            - name: DATABASE_USER
              valueFrom:
                secretKeyRef:
                  name: surrealdb-secrets
                  key: rootUser
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: surrealdb-secrets
                  key: rootPassword
