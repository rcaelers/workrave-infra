apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: surrealdb-tailnet
spec:
  defaultBackend:
    service:
      name: surrealdb
      port:
        name: http
  ingressClassName: tailscale
  tls:
    - hosts:
        - surrealdb
