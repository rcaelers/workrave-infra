apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - argocd-rbac-cm-patch.yaml
  - pocket-id-setup-rbac.yaml
  - argocd-server-deployment-patch.yaml

patches:
  - path: argocd-cm-patch.yaml
