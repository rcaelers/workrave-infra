apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: cert-manager-linode-webhook
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/linode/cert-manager-webhook-linode.git
  ref:
    tag: v0.4.1
