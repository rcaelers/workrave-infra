apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: guardrail-prd
resources:
  - ../../base
configMapGenerator:
  - name: guardrail-pocket-id-config-config
    options:
      disableNameSuffixHash: true
    files:
      - setup-config.yaml
