apiVersion: apps/v1
kind: Deployment
metadata:
  name: guardrail-processor
spec:
  template:
    spec:
      containers:
        - name: guardrail-processor
          env:
            - name: RUST_LOG
              value: "guardrail=info,apalis=info,apalis_redis=info"
