apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: guardrail
resources:
  - ../../base
configMapGenerator:
  - name: guardrail-config
    behavior: merge
    files:
      - 01-ingress.yaml
      - 01-web.yaml
